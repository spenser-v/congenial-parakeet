def hello(name): #defining function = creating it
    print ("Hello, " + name)

hello("Alice")
#calling the created function; PASSING "Alice" through the function;
#"Alice" is the argument; name is the parameter
hello("Bob")